import { TestBed, async, inject } from '@angular/core/testing';

import { AdminRestrictionsGuard } from './admin-restrictions.guard';

describe('AdminRestrictionsGuard', () => {
  // beforeEach(() => {
  //   TestBed.configureTestingModule({
  //     providers: [AdminRestrictionsGuard]
  //   });
  // });

  // it('should ...', inject([AdminRestrictionsGuard], (guard: AdminRestrictionsGuard) => {
  //   expect(guard).toBeTruthy();
  // }));
});
