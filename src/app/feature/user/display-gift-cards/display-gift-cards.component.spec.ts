import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DisplayGiftCardsComponent } from './display-gift-cards.component';

describe('DisplayGiftCardsComponent', () => {
  // let component: DisplayGiftCardsComponent;
  // let fixture: ComponentFixture<DisplayGiftCardsComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ DisplayGiftCardsComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(DisplayGiftCardsComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
