import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PaymentothersComponent } from './paymentothers.component';

describe('PaymentothersComponent', () => {
  // let component: PaymentothersComponent;
  // let fixture: ComponentFixture<PaymentothersComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ PaymentothersComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(PaymentothersComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
