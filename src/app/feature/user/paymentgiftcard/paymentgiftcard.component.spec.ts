import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PaymentgiftcardComponent } from './paymentgiftcard.component';

describe('PaymentgiftcardComponent', () => {
  // let component: PaymentgiftcardComponent;
  // let fixture: ComponentFixture<PaymentgiftcardComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ PaymentgiftcardComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(PaymentgiftcardComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
